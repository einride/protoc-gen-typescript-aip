version: v1

name: buf.build/einride/protoc-gen-typescript-aip

deps:
  - buf.build/einride/googleapis

lint:
  use:
    - DEFAULT
  except:
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
    - RPC_REQUEST_RESPONSE_UNIQUE
    - ENUM_VALUE_PREFIX
